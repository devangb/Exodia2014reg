<!DOCTYPE html>
<html>
  <head>
  Organizers only zone
  </head>
  <body>
  <hr />

  <% if signed_in? %>
  
      <div class="btn-group">
      <button type="button">Databas<%= link_to "e", current_user %></button><br />
      <%= link_to "Sign out", destroy_user_session_path, method: "delete" %> 
    </div>
    <% else %>
      <%= link_to "Sign in", new_user_session_path %>
    <%end%>
  </body>
</html>